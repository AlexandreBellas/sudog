/**
 * The number of blocks per row/column of the game.
 */
export const boardSize = 3 // 3x3 blocks

/**
 * The number of tiles present in each block.
 */
export const blockSize = 3 // 3x3 tiles

export const winTimeoutDelayMs = 1000

export const loseTimeoutDelayMs = 500
